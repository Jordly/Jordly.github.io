# 2026-07-07 工作日志

## 项目对比弹窗乱码修复

### 问题
用户选中两个项目点击"开始对比"后，弹窗显示区域出现乱码（如 `鍒?/span>`、`缁村害`、`宸㈡紓`）。

### 根因
这些中文字符在 Git 历史中曾被错误保存为 GBK/GB2312 编码（中文被当 UTF-8 解析），典型 mojibake：
- `鍒?` 应为 `分`（综合得分单位）
- `缁村害` 应为 `维度`（表头）
- `宸㈡紓` 应为 `差距`（表头）
- `鎵归噺缂栬緫鍔熻兘寮€鍙戜腑` 应为 `批量编辑功能开发中`
- `鏃犲巻鍙叉暟鎹?` 应为 `无历史数据时返回平直线段`（注释）

### 修复
在 `app.js` 中逐一替换（涉及两处对比模态函数 + 其他位置），全部改为正确 UTF-8 中文。

**版本号**：v202607070920
**Git提交**：`cdcdd03`

## 核心知识能量池版块优化（方案A+B+C+D）

### 用户提出的4个问题
1. 卡片/标签点击无效（原 showKnowledgeDetail 只是 alert）
2. 卡片文字太多太乱，不便于快速定位
3. 内容应定位"管理者通用技能"，不是一线基础客服内容
4. 必须与系统数据管理"知识条目表"打通同源

### 改动清单
- **方案A（交互）**：`showKnowledgeDetail()` 改为自定义弹窗（含完整描述、标签、编辑/删除）；标签点击 `kypFilterByTag()` 实现筛选；添加/编辑/删除按钮接入 `addKnowledge/editKnowledge/deleteKnowledge/showKnowledgeForm/submitKnowledgeForm`，写回 KNOWLEDGE + saveKnowledge()。
- **方案B（UI精简）**：卡片去掉 scope/perm badge 与底部 tag 群；描述改为1行截断（`.kyp-card-short`，用新增 short 字段）；卡片高度明显缩小。
- **方案C（内容升级）**：`DEFAULT_KNOWLEDGE` 8条默认数据改为管理视角（人力成本优化、AI工具落地、经验复制SOP、离职应急、升级投诉沟通、新任主管上手、BPO费效比、智能质检），新增 `category`（管理方向：团队管理/成本控制/效率提升/风险防控/体系搭建/沟通协作）与 `short` 字段。
- **方案D（数据打通）**：
  - **修复 `initKnowledge()` bug**（if块为空，每次刷新用默认覆盖用户数据）→ 改为带种子版本号 `KNOWLEDGE_SEED_VERSION='2026-07-manager'`，本地有同版本数据才加载，否则重新播种。
  - 知识条目表 `SYSTEM_DATA_TABLES.knowledge.fields` 扩展为 type/category/short/description/tags/scope/permission 等，type 选项与能量池统一（6类）。
  - colDefs 增加"管理方向/浏览/下载"列，加 `goEnergyPool:true`；详情页加"📖 在能量池查看"按钮跳转 renderModule('knowledge')。
  - 两侧共用 `KNOWLEDGE` 数组引用，双向实时同步。

### 注意
- 因 initKnowledge 历史 bug，用户此前无法持久保存知识数据（刷新即被默认覆盖）。修复后旧默认数据会被新管理视角种子替换——符合用户"内容升级"诉求。
- 涉及文件：app.js、premium-enhancements.css、index.html（版本号 v202607071050）、docs/ 同步三份。

**版本号**：v202607071050
**Git提交**：`d77a3ce`

## 核心知识能量池 · 后续小修（筛选失效 + 图例美化）

### 用户反馈2个问题
1. 分类标签栏（全部/SOP流程优化...）点击无筛选效果
2. 底部图例颜色与卡片 badge 不一致，且图例太小太粗糙；希望图例不要离卡片太远显得突兀

### 根因1（筛选失效）
`kypFilter`/`kypSearch`/`kypFilterByTag` 原本写在 `renderKnowledge()` 返回的 HTML 模板字符串里的 `<script>` 标签中。用 innerHTML 插入 DOM 时 `<script>` **不会执行**，所以函数不存在，点击标签等于调用未定义函数 → 无反应。
**修复**：把三个函数移出模板字符串，改为全局函数（位于 `showKnowledgeDetail` 之前）。
**连带教训**：删除模板内 `<script>` 块时，误把主模板字符串的闭合反引号 `` `; `` 一起删掉，导致 renderKnowledge 返回字符串"吞掉"后续所有代码，node --check 报 renderHandover 内部 `Unexpected token 'class'`。已在函数结尾补回 `` `; ``。

### 根因2（图例不一致+粗糙）
- 图例用了6个独立硬编码色值（如 `#378ADD`），与卡片 `.ktp-xxx` 实际色（SOP 用 `#185FA5`）对不上。
- 图例样式仅 8px 小方块 + 2px 圆角 + 12px 字，粗糙。
**修复**：
- 把图例从底部独立区块**移入筛选栏最右侧**，与"全部/分类"标签同处一行，间距自然不突兀。
- 颜色统一改用卡片 badge 同款主色（#185FA5/#A32D2D/#3B6D11/#854F0B/#7C3AED/#666）。
- 样式美化：圆点放大到 10px、border-radius:50%、加浅灰背景胶囊容器、字号11px。

### 涉及文件
app.js（删除模板内script+补反引号+全局函数+图例移入筛选栏）、premium-enhancements.css（`.kyp-legend`/`.kyp-legend-item`/`.kyp-legend-dot` 重写）、index.html（版本号 v202607071145）、docs/ 同步三份。

**版本号**：v202607071145
**Git提交**：`6170b79`

## 核心知识能量池 · 卡片背景美化（左侧色条+浅色底）

### 用户反馈
8张卡片白底黑字太单调，缺少层次感。

### 修复
只改 `premium-enhancements.css` 中 `.kyp-card` 样式：
1. 每张卡加 **3px 左侧色条**，颜色与分类 badge 一致（SOP蓝/风控红/成本绿…）
2. 背景从纯白改为各分类对应的**极浅底色**（如 SOP=#f8fbff, 风控=#fff9f9）
3. hover 阴影增强

### 涉及文件
premium-enhancements.css、index.html（版本号 v202607071400）、docs/

**Git提交**：`d810688`

## 核心知识能量池 · 后续优化续（防填充/删图例/拖拽排序）

### 1. 搜索框被浏览器自动填充为登录账号（v202607071415）
- 根因：浏览器把知识搜索框误识别为"用户名"字段，自动填入 admin。
- 修复：搜索框加 `autocomplete="off"` 初版无效（Chrome 忽略），后续强化为 `autocomplete="nope"` + 改名 id/name（`kyp-search-field`/`kyp_search_query`）+ `type="search"` + `spellcheck="false"`（v202607071455 一并处理）。

### 2. 删除筛选栏图例（v202607071420）
- 用户认为图例无意义，删掉筛选栏右侧 SOP/风控/成本… 图例块。删图例时不慎把搜索框的 `autocomplete="off"` 一并误删（恢复成可自动填充），在 v202607071455 的强化防填充中已重新修正。

### 3. 卡片拖拽排序（v202607071455 初版 + v202607071505 修复）
- 初版实现：卡片 `draggable="true"` + `ondragstart/ondragend`，grid `ondragover/ondrop`；`kypDragStart/kypDragOver/kypDragEnd/kypDrop` 全局函数；拖拽后 splice 重排 KNOWLEDGE + saveKnowledge + renderModule('knowledge')。
- **bug（v202607071505 修复）**：`kypDragStart` 用 `e.target.dataset.id`，从卡片内文字/子元素拖动时 `e.target` 是子元素（无 data-id）→ `parseInt(undefined)=NaN` → drop 里 `if(!kypDragSrcId) return` 因 `!NaN===true` 永远跳过排序，松手无变化。
- **修复**：dragstart 改用 `e.currentTarget`（绑定在卡片上，必带 data-id）；drop 兜底取 `.kyp-drag-over` 高亮卡片，避免松手在缝隙落空。
- **教训**：HTML5 拖拽事件中取源元素 id 必须用 `e.currentTarget` 而非 `e.target`。

### 涉及版本/提交
- v202607071415 `...` 防填充初版
- v202607071420 删图例
- v202607071455 防填充强化 + 拖拽初版
- v202607071505 `f9b2122` 拖拽修复（仅改 currentTarget，用户反馈仍无效）

### 4. 搜索框防填充最终方案 + 拖拽重做（v202607071530 `da144a0`）
- **搜索框**：`autocomplete="nope"` 在 Chrome 下仍被忽略。最终改为 `autocomplete="off"` + `readonly` + `onfocus="this.removeAttribute('readonly')"`——浏览器不填充只读输入框，用户点击聚焦时才可输入。这是可靠的防填充方案。
- **拖拽**：放弃"drop 后 splice 数组 + renderModule 重渲染"的链路（中间环节任一异常都会导致顺序不变）。改为 drop 时用 DOM `insertBefore` **直接移动卡片节点**（所见即所得），再由 DOM 顺序反推 `KNOWLEDGE` 数组顺序 `KNOWLEDGE.sort(...)`，最后 `saveKnowledge()`。视觉反馈改用 `.kyp-dragging`（半透明）/`.kyp-drag-over`（蓝色高亮）类名，不再用 inline opacity。
- **教训**：HTML5 拖拽排序最稳妥做法是"drop 时直接 DOM 移动 + 反推数组"，不要依赖整页 JS 重渲染来体现顺序变化。
