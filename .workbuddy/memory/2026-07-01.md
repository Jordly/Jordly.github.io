# 2026-07-01 工作日志

## 修复的3个问题

### 问题1：项目总览看板空白（已修复）
- **根因**：app.js `updateUserDisplay()` 函数使用了未定义变量 `firstChar`，导致 JS 报错 → `renderModule("dashboard")` 从未执行
- **修复**：在 app.js 第837行后添加 `const firstChar = (displayName || '').charAt(0).toUpperCase() || '?';`
- **影响文件**：app.js, docs/app.js

### 问题2：筛选栏布局优化（已修复）
- **第一行**：4个筛选项（时间/职场/类型/状态）改为 `flex:1` 铺开至 75% 宽度，不再挤在左边
- **第二行**：5个筛选项（平台/品类/品牌/运营PM/客服负责）+ 高级筛选按钮，默认折叠隐藏
- **关键修改**：高级筛选按钮从第二行移到第一行末尾（始终可见），否则用户无法点击展开
- **影响文件**：premium-enhancements.css（`.filter-row-v4` 宽度75% + `.fb-select` flex:1）, app.js（按钮位置调整）

### 问题3：右上角头像下拉菜单（随问题1一起修复）
- 根因同样是 `firstChar` 未定义，修复后头像正常显示
- 功能确认：个人设置→跳转个人基础设置、切换账号、退出账号 均正常

## 提交记录
- Commit 1: 821d89c - 修复3个问题：看板空白+筛选栏布局+头像显示
- Commit 2: 5f1a52d - fix: 更新缓存版本号，确保CDN和浏览器拉取新文件
- 已推送到 GitHub main 分支

## 踩坑记录：缓存版本号
- **问题**：用户 Ctrl+Shift+Del 清 cookies 后页面仍无变化
- **根因**：index.html 中 script/link 的 `?v=xxx` 版本号没更新，Cloudflare CDN 和浏览器缓存都认为文件没变
- **教训**：修改 app.js/premium-enhancements.css 后必须同步更新 index.html 中的版本号

### 坑3：Cloudflare Pages 构建失败（子模块）
- **根因**：backups/ 和 prototype/ 目录含嵌套 .git，被当作子模块追踪，导致构建失败
- **修复**：git rm --cached 移除子模块 + .gitignore 禁止再提交

### 问题2：最近登录表格 + admin个人信息
- admin默认信息恢复：姓名周东利、昵称Jordly、职位客服总监等截图内容
- 最近登录改为4列表格：时间 | 设备（PC/手机/平板）| 操作系统（含HarmonyOS）| 状态（成功/失败）
- 登录失败也会记录
- 影响文件：app.js, login.html, style.css

## 提交记录
- **问题**：任意账号密码都能登录，还自动给超级管理员权限
- **根因**：login.html 的 doLogin() 不验证密码，找不到用户就自动创建假用户
- **修复**：验证密码+用户不存在/密码错误都报错+内置默认用户兜底

## 提交记录
- Commit 1: 821d89c - 修复3个问题：看板空白+筛选栏布局+头像显示
- Commit 2: 5f1a52d - fix: 更新缓存版本号
- Commit 3: 8e9a94a - fix: 移除backups子模块
- Commit 4: f3456d0 - fix: 移除prototype子模块
- Commit 5: 91a3a93 - fix: 修复致命安全漏洞 - 任意账号密码都能登录

## 修复：筛选栏第二行还是不显示（17:48）
### 问题
用户反馈点击"高级筛选"按钮后第二行无反应，控制台有5个错误和3条警告。

### 根因
`app.js` 中有 **3个 `toggleAdvancedFilter()` 函数**：
- 第1个（第2370行）：正确的版本，用 `style.display` 控制显隐
- 第2个（第9651行）：旧版本，用 `_advancedFilterOpen` + `renderModule`
- 第3个（第9857行）：又一个旧版本（重复）

JS 中最后一个定义的函数覆盖前面的，所以第3个（旧版）生效。

### 修复
后面2个旧函数替换为与第1个相同的新实现（`getElementById` + `style.display`）。

### 修改文件
- `app.js`：替换第9651行和第9857行的旧函数
- `index.html`：版本号 `v=202607011440` → `v=202607011500`
- `premium-enhancements.css`：版本号同步更新

### 同步
- 根目录 GitHub commit: 411707f ✅
- 已同步到 prototype/ ✅

## 筛选栏3项优化（15:10）
### 用户需求
1. 第二行5个筛选框总长度与第一行4个一致对齐
2. 第二行默认收起，点击高级筛选展开
3. 可手动收起或F5刷新自动收起

### 修改内容
1. `.filter-row-v4-second`：添加 `width: 75% !important` + `display:flex` + `flex-wrap:nowrap`，与第一行对齐
2. `.fb-search-wrap`：从 `inline-flex; flex-shrink:0` 改为 `flex: 1`，5个筛选项均匀铺开
3. `.fb-search-trigger`：从 `inline-flex` 改为 `flex: 1; width: 100%`
4. 删除残留的 `.filter-row-v4-advanced { display:none!important }` 规则
5. 版本号 → `v=202607011510`

### 同步
- 根目录 commit: 8e80ffc ✅ push成功
- prototype 同步完成 ✅

## 筛选栏修复 - 第二轮（15:20）
### 问题
1. 默认第二行展开——CSS `display:flex !important` 覆盖了内联 `style="display:none"`
2. 点击高级筛选偶尔变"收起筛选"但点不了收起
3. gap:12px 太大，5个框间距过宽

### 根因
`.filter-row-v4-second` 中 `display: flex !important` 优先级高于 HTML 内联 style，导致 JS 设 display:none 无效

### 修复
1. `.filter-row-v4-second` **删除** `display:flex !important` 和 `flex-wrap:nowrap !important`，显隐完全由JS内联style控制
2. gap 从 12px 缩小到 8px
3. 版本号 → `v=202607011520`
4. 根目录 commit: 62ea9f4 ✅ / prototype 同步完成 ✅

## 筛选栏修复 - 第三轮（15:30）彻底修复
### 问题
1. 第二行仍然默认展开——**真正根因**：div同时有`filter-row-v4`和`filter-row-v4-second`，`.filter-row-v4`的`display:flex!important`优先级最高
2. 点击无反应；需加"更多"按钮对齐"高级筛选"

### 彻底方案
1. CSS：`.filter-row-v4-second` 加 `display:none!important`（双类选择器优先级 > 单类）
2. JS：`setProperty('display','flex/none','important')` 切换，3个函数全部统一
3. 第二行末尾新增 `<button class="fb-more-btn">更多 ▼</button>`
4. 按钮文字同步：展开→收起筛选▲+更多▲，收起→高级筛选▼+更多▼
5. 版本号 v=202607011530 | commit: 5726c7a ✅ | prototype ✅

## 刷新保留当前模块（15:45）
### 需求
刷新浏览器后停留在当前导航页，不跳回项目总览看板

### 方案
1. `renderModule()` 中加 `localStorage.setItem('cs_lastModule', module)` 保存当前模块
2. 初始化时2处 `renderModule("dashboard")` 改为 `renderModule(localStorage.getItem('cs_lastModule') || "dashboard")`

### 踩坑
第1次只改了1处（from=login路径），遗漏了checkLogin后的第2处 → 用户测试无效
### 补充修复（15:55）
3. 导航栏高亮不同步：内容页对了但左侧导航还高亮"项目总览看板"
   - **原因**：`renderModule()` 只渲染内容区，不更新导航栏active状态
   - **修复**：在 `renderModule()` 开头加导航栏高亮同步逻辑
4. 版本号 v=202607011555 | commit: 9eaf400 ✅

## 系统数据管理页面美化（16:48）
### 修改内容
1. **卡片目录页**：去掉`::before`和`::after`装饰圆形（右上和左下），图标微调阴影
2. **9个数据表格**：按钮从大红矩形改为文字链接样式（✏️编辑蓝字 + 🗑删除浅红字）
3. **表格整体**：渐变极光色表头 + 斑马纹（隔行淡灰） + 行hover高亮
4. **顶部操作栏**：按钮统一圆角8px + 渐变蓝色
5. **搜索框**：圆角、聚焦描边优化
6. 版本号 v=202607011640 | commit: a710f3e ✅ | prototype ✅

## 系统用户管理3项优化（17:15）
### 1. 新增用户改为自定义弹窗
- 替换浏览器原生prompt()，改为白色圆角卡片弹窗，含姓名/账号/密码/角色4个输入框
- 底部"取消"+"确定创建"两个按钮

### 2. 统一角色列表
- `editUserRole()` 的角色列表改为用 `ROLES` 数组
- 删除"客服组长""技术伙伴""风控伙伴"三个不存在的角色

### 3. 替换4张统计卡片为团队角色分布图
- 去掉"全部用户/已激活/待审核/已禁用"4张重复卡片
- 换上横向角色分布条形图：超级管理员/管理员/客服总监/客服经理/客服主管/项目伙伴
- 每人对应一个彩色进度条 + 人数 + 百分比

### 涉及文件
- app.js：showAddUser重写 + editUserRole角色列表 + renderNotifications角色分布
- 版本号 v=202607011715 | commit: 5001703 ✅ | prototype ✅

## 系统用户管理 - 原生弹窗彻底替换（17:37）
### 问题
点击"修改角色"确定后弹出浏览器原生黑色 alert 弹窗（`已修改 xxx 的角色为：xxx`），很丑

### 排查结果 - 系统用户管理模块共残留 **11处** 原生弹窗：
| 函数 | 原生弹窗 | 替换为 |
|---|---|---|
| approveUser | 3个alert() | showToast() |
| confirmEditRole | 1个alert() | showToast() ← 用户截图看到的 |
| resetUserPassword | 1个prompt()+2个alert() | showPromptModal()+showToast() |
| disableUser | 1个alert()+1个confirm() | showToast()+showConfirmModal() |
| deleteUser | 1个alert()+1个confirm() | showToast()+showConfirmModal() |
| showAddUser | 1个alert() | showToast() |
| confirmAddUser | 2个alert() | showToast() |

### 新增函数
- `showConfirmModal(msg, title, onConfirm, onCancel)` — 自定义确认弹窗（白底圆角+渐变蓝按钮+动画）
- `showPromptModal(title, label, defaultValue, onConfirm)` — 自定义输入弹窗（替代原生prompt）

### 新增CSS
- style.css 末尾追加：`.sd-confirm-overlay/.sd-confirm-box/.sd-prompt-overlay/.sd-prompt-box` 完整样式

### 涉及文件
- app.js：替换11处原生弹窗 + 新增2个自定义弹窗函数
- style.css：新增弹窗CSS样式
- index.html：版本号 v=202607011737

### 同步
- commit: 9b8dc58 ✅ push成功
- prototype: ad30fe0 ✅ 已同步
